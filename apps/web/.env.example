# Supabase — get dev project values from the team / Supabase dashboard
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# The public URL of this deployment (used for auth redirects, OG tags, etc.)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
